

{# {%
  set classes = [
    'block',
    'block-' ~ configuration.provider|clean_class,
    'block-' ~ plugin_id|clean_class,
    layout ? 'layout--' ~ layout|clean_class,
  ]
%} #}

{# <div{{ attributes.addClass(classes) }}> #}

{# {{ kint () }} #}
  
  {% block content %}
  	{{ attach_library('lelohex/productscommerce-css') }}
    <div{{ content_attributes.addClass('commerceTitle') }}>
      {{ content }}
    </div>
  {% endblock %}
{# </div> #}
